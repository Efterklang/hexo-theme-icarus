{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "/common/theme_selector.json",
    "description": "Show the theme selector",
    "type": "object",
    "properties": {
        "enable": {
            "type": "boolean",
            "default": true,
            "description": "Enable theme selector in the navbar"
        }
    }
}