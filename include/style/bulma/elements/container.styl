$container-offset ?= (2 * $gap)

.container
  flex-grow 1
  margin 0 auto
  position relative
  width auto
  +widescreen()
    max-width $widescreen - $container-offset
  +fullhd()
    max-width $fullhd - $container-offset
